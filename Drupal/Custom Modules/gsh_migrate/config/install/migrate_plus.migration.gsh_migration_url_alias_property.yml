id: gsh_migration_url_alias_property
label: 'Migrate URL Aliases for Property from the csv file'
migration_group: gsh_migration
source:
  plugin: csv
  # Full path to the file.
  path: 'modules/custom/gsh_migrate/assets/csv/url-alias-node.csv'
  header_row_count: 1
  keys:
    - nid
process:
  # Adding the mapping between the fields and the csv columns.
  source: source
  alias: path
  langcode:
    plugin: default_value
    default_value: und
destination:
  plugin: 'url_alias'
migration_dependencies:
  required: {}
dependencies: { }
